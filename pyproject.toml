[tool.poetry]
name = "ivekhov_gendiff"
version = "0.1.0"
description = ""
authors = ["ivekhov"]
packages = [
  { include = "gendiff" },
]


[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]
flake8 = "^3.8"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"

[tool.poetry.scripts]
gendiff = 'gendiff.scripts.gendiff:main'